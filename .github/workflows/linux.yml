name: Linux

on: [push]

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4
    - uses: swift-actions/setup-swift@v2
      with:
        swift-version: '6.1'
    - name: Build
      run: swift build
    - name: Test
      run: swift test
